#include <stdio.h>
#include<stdlib.h>

int main(void){
    int n, a, b, c;
    char k[100][3];
    scanf("%d", &n);
    for (int i = 0; i < n; i++){
        scanf("%d %d %d", &k[i][0], &k[i][1], &k[i][2]);
    }

    

}