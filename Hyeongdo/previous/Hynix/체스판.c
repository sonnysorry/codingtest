#include <stdio.h>

int main(void){
    int n, m;
    scanf("%d %d", &n, &m);
    int arr[50][50] = {0, };
    for(int i = 0; i < m; i++){
        for(int j = 0; j < n; j++){
            scanf("s", &arr[j][i]);
        }
    }
    max = 0;
    for(int p = 0; p < n- 7; p++){
        for(int q = 0; q < m-7; q++){
                
            }
        }
    }
}